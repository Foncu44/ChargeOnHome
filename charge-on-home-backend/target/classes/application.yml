server:
  port: 8080

spring:
  application:
    name: charge-on-home-backend
  
  datasource:
    url: jdbc:mysql://localhost:3306/charge_on_home?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=Europe/Madrid&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=utf8
    username: root
    password: A.Foncu4848
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

# Configuración JWT
jwt:
  secret: mySecretKey123456789012345678901234567890
  expiration: 86400000 # 24 horas en milisegundos

# Configuración CORS
cors:
  allowed-origins: http://localhost:4200
  allowed-methods: GET,POST,PUT,DELETE,OPTIONS
  allowed-headers: "*"

# Configuración de logging
logging:
  level:
    com.chargeonhome.backend: DEBUG
    org.springframework.security: DEBUG 